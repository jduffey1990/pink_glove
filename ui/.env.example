# Where the Django API lives, for the DEV SERVER. A production build leaves
# this unset: it is served by the API itself (ADR-027) and calls it with
# relative URLs. Set it only for a frontend hosted on a different origin, which
# then also needs that origin in the backend's CORS_ALLOWED_ORIGINS.
VITE_API_BASE_URL=http://localhost:8000
