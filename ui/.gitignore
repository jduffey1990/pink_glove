# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

# Generated from the backend by `npm run api:types`, but COMMITTED on purpose
# (ADR-019): a backend change then shows up as a reviewable diff here instead
# of silently breaking someone else's build. Listed so nobody "tidies" them
# into this file later.
!openapi.yaml
!src/api/schema.d.ts
