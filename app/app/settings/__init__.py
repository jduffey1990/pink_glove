# Settings are selected via DJANGO_SETTINGS_MODULE, one of:
#   app.settings.local        (manage.py default)
#   app.settings.test         (pytest default, see pyproject.toml)
#   app.settings.production
